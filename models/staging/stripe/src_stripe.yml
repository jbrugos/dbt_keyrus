sources:
  - name: striping
    description: The below table is available in a different schema to the shop data.
    database: DBT
    schema: stripe
    tables:
      - name: payment
        freshness:
          warn_after:
            count: 100
            period: day
          error_after:
            count: 120
            period: day
        loaded_at_field: _BATCHED_AT
