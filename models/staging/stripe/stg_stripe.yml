version: 2

models:
  - name: payment
    columns:
      - name: paymentmethod
        tests:
          - accepted_values:
              values:
                - credit_card
                - bank_transfer
                - coupon
                - gift_card
