version: 2

models:
  - name: payment
    columns:
      - name: PAY_STATUS
        tests:
          - accepted_values:
              values:
                - creadit_card
                - bank_transfer
                - coupon
                - gift_card
