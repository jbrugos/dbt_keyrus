version: 2

models:
  - name: payment
    description: stripe order payment.
    columns:
      - name: paymentmethod
        description: "{{ doc('payment_method') }}"
        tests:
          - accepted_values:
              values:
                - credit_card
                - bank_transfer
                - coupon
                - gift_card
