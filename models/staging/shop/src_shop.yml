sources:
  - name: SnowKeyrus
    database: DBT
    schema: SHOP
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: orders